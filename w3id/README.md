# w3id registration drafts

One-time PR to <https://github.com/perma-id/w3id.org> adding these two
directories. DELIBERATELY NOT SUBMITTED yet — pending: GitHub Pages enabled on
ND-DAC-DOME/paper-anatomy, target URLs confirmed, and a one-time OOPS! run
(reviewer recommendation before public release).

- `paper-anatomy/` — the vocabulary namespace (vocab#, shapes#, releases/)
- `alexandria/` — the application's instance-data namespace
